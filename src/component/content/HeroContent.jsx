import React from "react";

const HeroContent = () => {
  return (
    <div id="home" style={{color: "#fff", backgroundColor: "#2e64aa", height: "100vh", display: "flex", justifyContent: "center", alignItems: "center"}}>
      HeroContent
    </div>
  );
};

export default HeroContent;
